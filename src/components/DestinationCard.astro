---
interface Props {
  title: string;
  slug: string;
  location: string;
  image: string;
  rating: string;
  price: string;
  gygLink: string;
  locale?: string;
}

const {
  title,
  slug,
  location,
  image,
  rating,
  price,
  gygLink,
  locale = 'en'
} = Astro.props;

const detailUrl = locale === 'en' ? `/experience/${slug}/` : `/${locale}/experience/${slug}/`;
---

<div class="group bg-[#0b1726] border border-slate-800 rounded-2xl overflow-hidden hover:border-[#c9a54e]/60 transition-all duration-300 hover:shadow-2xl hover:shadow-[#c9a54e]/10 flex flex-col h-full">
  
  <!-- Image Container -->
  <div class="relative aspect-[16/10] overflow-hidden bg-slate-900">
    <img
      src={image}
      alt={title}
      loading="lazy"
      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500 ease-out"
    />
    <div class="absolute inset-0 bg-gradient-to-t from-[#0b1726] via-transparent to-black/30"></div>
    
    <!-- Top Badges -->
    <div class="absolute top-3.5 left-3.5 right-3.5 flex items-center justify-between">
      <span class="bg-[#07111e]/90 backdrop-blur-md text-[#c9a54e] text-[11px] font-bold uppercase tracking-wider px-3 py-1 rounded-full border border-[#c9a54e]/30 shadow-md">
        {location}
      </span>
      <span class="bg-[#07111e]/90 backdrop-blur-md text-amber-400 text-xs font-semibold px-2.5 py-1 rounded-full border border-slate-700 shadow-md flex items-center space-x-1">
        <span>★</span>
        <span>{rating.split(' ')[0]}</span>
      </span>
    </div>

    <!-- Bottom Price Overlay -->
    <div class="absolute bottom-3 left-3.5">
      <span class="text-[11px] text-slate-300 block font-medium">Starting from</span>
      <span class="text-base font-bold text-white font-serif">{price}</span>
    </div>
  </div>

  <!-- Content Body -->
  <div class="p-5 flex flex-col flex-grow justify-between">
    <div>
      <h3 class="font-serif text-lg font-bold text-white group-hover:text-[#c9a54e] transition-colors leading-snug mb-2 line-clamp-2">
        <a href={detailUrl}>
          {title}
        </a>
      </h3>
      <p class="text-xs text-slate-400 line-clamp-2 leading-relaxed mb-4">
        {rating} · Curated VIP priority skip-the-line access & 24h free cancellation.
      </p>
    </div>

    <!-- Actions -->
    <div class="pt-4 border-t border-slate-800/80 flex items-center justify-between gap-3">
      <a
        href={detailUrl}
        class="text-xs font-bold text-slate-300 hover:text-white flex items-center space-x-1 transition-colors"
      >
        <span>Read Guide</span>
        <span>→</span>
      </a>

      <a
        href={gygLink}
        target="_blank"
        rel="nofollow sponsored noopener"
        class="bg-[#ff7043] hover:bg-[#f4511e] text-white text-xs font-bold px-3.5 py-2 rounded-lg shadow-md shadow-[#ff7043]/20 hover:shadow-[#ff7043]/40 transition-all text-center"
      >
        GetYourGuide →
      </a>
    </div>

  </div>

</div>
