---
interface Props {
  currentLocale?: string;
}

const { currentLocale = 'en' } = Astro.props;

const placeholders: Record<string, { 
  search: string; 
  date: string; 
  guests: string; 
  btn: string; 
  all: string; 
  europe: string; 
  asia: string; 
  islands: string; 
  safari: string; 
  me: string; 
  wellness: string; 
  hotels: string;
  found: string;
  loadMore: string;
  showing: string;
}> = {
  en: {
    search: "Search 1,000+ luxury places, tours, private yachts...",
    date: "Anytime",
    guests: "1 - 2 Guests",
    btn: "Search",
    all: "✨ All (1,000)",
    europe: "🇫🇷 Europe",
    asia: "🇯🇵 Asia",
    islands: "🇲🇻 Islands",
    safari: "🦒 Safari",
    me: "🇦🇪 Middle East",
    wellness: "🌿 Wellness Spas",
    hotels: "👑 5-Star Sanctuaries",
    found: "Sovereign Experiences Found",
    loadMore: "Load More Experiences ↓",
    showing: "Showing"
  },
  vi: {
    search: "Tìm kiếm 1.000+ điểm đến, du thuyền, trải nghiệm thượng lưu...",
    date: "Thời gian: Bất kỳ",
    guests: "1 - 2 Khách",
    btn: "Khám Phá",
    all: "✨ Tất Cả (1.000)",
    europe: "🇫🇷 Châu Âu",
    asia: "🇯🇵 Châu Á",
    islands: "🇲🇻 Đảo Ngọc",
    safari: "🦒 Safari Châu Phi",
    me: "🇦🇪 Trung Đông",
    wellness: "🌿 Spa & Trị Liệu",
    hotels: "👑 Khách Sạn 5 Sao",
    found: "Hành Trình Thượng Lưu Được Tìm Thấy",
    loadMore: "Khám Phá Thêm Trải Nghiệm ↓",
    showing: "Đang hiển thị"
  }
};

const t = placeholders[currentLocale] || placeholders.en;
---

<div class="w-full max-w-4xl mx-auto mb-12">
  <!-- GYG-Style Floating Search Bar Pill -->
  <div class="bg-[#0b1726]/95 backdrop-blur-xl border border-slate-700/80 rounded-2xl sm:rounded-full p-2.5 shadow-2xl shadow-black/50 transition-all duration-300 hover:border-[#c9a54e]/60">
    <div class="flex flex-col sm:flex-row items-center gap-2 sm:gap-0">
      
      <!-- Input 1: Destination / Experience Keyword Search -->
      <div class="flex items-center flex-grow w-full px-4 py-2 sm:py-1 relative">
        <span class="text-slate-400 text-lg mr-3">🔍</span>
        <input
          id="global-destination-search"
          type="text"
          placeholder={t.search}
          class="w-full bg-transparent border-none text-white placeholder-slate-400 text-sm font-medium focus:outline-none focus:ring-0 pr-8"
          autocomplete="off"
          aria-label={t.search}
        />
        <!-- Quick Clear Button (X) -->
        <button
          id="search-clear-btn"
          type="button"
          class="hidden absolute right-3 text-slate-400 hover:text-white text-xs w-5 h-5 rounded-full bg-slate-800 flex items-center justify-center transition-all"
          title="Clear search"
          aria-label="Clear search"
        >
          ✕
        </button>
      </div>

      <!-- Divider (Desktop) -->
      <div class="hidden sm:block w-[1px] h-8 bg-slate-700/80 mx-1"></div>

      <!-- Dropdown 2: Date Selector (Visual Luxury Touch) -->
      <div class="hidden md:flex items-center px-4 py-1 text-slate-300 text-xs font-medium cursor-pointer hover:text-white transition-colors">
        <span class="mr-2">📅</span>
        <span>{t.date}</span>
        <span class="text-slate-500 ml-2 text-[10px]">▼</span>
      </div>

      <!-- Divider (Desktop) -->
      <div class="hidden md:block w-[1px] h-8 bg-slate-700/80 mx-1"></div>

      <!-- Dropdown 3: Guests (Visual Luxury Touch) -->
      <div class="hidden lg:flex items-center px-4 py-1 text-slate-300 text-xs font-medium cursor-pointer hover:text-white transition-colors">
        <span class="mr-2">👥</span>
        <span>{t.guests}</span>
        <span class="text-slate-500 ml-2 text-[10px]">▼</span>
      </div>

      <!-- Search Action Button -->
      <button
        id="search-trigger-btn"
        type="button"
        class="w-full sm:w-auto bg-gradient-to-r from-[#ff7043] to-[#f4511e] hover:from-[#f4511e] hover:to-[#e64a19] text-white font-bold text-xs sm:text-sm uppercase tracking-wider px-7 py-3.5 rounded-xl sm:rounded-full shadow-lg shadow-[#ff7043]/30 transition-all duration-300 transform active:scale-95 flex items-center justify-center space-x-2"
      >
        <span>{t.btn}</span>
        <span>→</span>
      </button>

    </div>
  </div>

  <!-- Quick Category Filter Pills (10 Sovereign Categories & Regions) -->
  <div class="flex items-center justify-center flex-wrap gap-2 pt-5" id="category-filter-pills">
    <button
      type="button"
      data-filter="all"
      class="filter-pill active px-3.5 py-1.5 rounded-full text-xs font-semibold bg-[#c9a54e]/20 border border-[#c9a54e] text-[#c9a54e] transition-all hover:border-[#c9a54e]"
    >
      {t.all}
    </button>
    <button
      type="button"
      data-filter="europe"
      class="filter-pill px-3.5 py-1.5 rounded-full text-xs font-semibold bg-slate-900/80 border border-slate-700 text-slate-300 transition-all hover:border-slate-500 hover:text-white"
    >
      {t.europe}
    </button>
    <button
      type="button"
      data-filter="asia"
      class="filter-pill px-3.5 py-1.5 rounded-full text-xs font-semibold bg-slate-900/80 border border-slate-700 text-slate-300 transition-all hover:border-slate-500 hover:text-white"
    >
      {t.asia}
    </button>
    <button
      type="button"
      data-filter="islands"
      class="filter-pill px-3.5 py-1.5 rounded-full text-xs font-semibold bg-slate-900/80 border border-slate-700 text-slate-300 transition-all hover:border-slate-500 hover:text-white"
    >
      {t.islands}
    </button>
    <button
      type="button"
      data-filter="safari"
      class="filter-pill px-3.5 py-1.5 rounded-full text-xs font-semibold bg-slate-900/80 border border-slate-700 text-slate-300 transition-all hover:border-slate-500 hover:text-white"
    >
      {t.safari}
    </button>
    <button
      type="button"
      data-filter="me"
      class="filter-pill px-3.5 py-1.5 rounded-full text-xs font-semibold bg-slate-900/80 border border-slate-700 text-slate-300 transition-all hover:border-slate-500 hover:text-white"
    >
      {t.me}
    </button>
    <button
      type="button"
      data-filter="wellness"
      class="filter-pill px-3.5 py-1.5 rounded-full text-xs font-semibold bg-slate-900/80 border border-slate-700 text-slate-300 transition-all hover:border-slate-500 hover:text-white"
    >
      {t.wellness}
    </button>
    <button
      type="button"
      data-filter="hotels"
      class="filter-pill px-3.5 py-1.5 rounded-full text-xs font-semibold bg-slate-900/80 border border-slate-700 text-slate-300 transition-all hover:border-slate-500 hover:text-white"
    >
      {t.hotels}
    </button>
  </div>

  <!-- Search Results Counter Status -->
  <div id="search-status-counter" class="text-center text-xs text-slate-400 mt-4 hidden" role="status" aria-live="polite">
    <span class="text-[#c9a54e] font-bold" id="counter-number">0</span>
    <span id="counter-label"> {t.found}</span>
  </div>
</div>

<script is:inline>
  function setup1000SearchAndFilters() {
    const searchInput = document.getElementById('global-destination-search');
    const clearBtn = document.getElementById('search-clear-btn');
    const searchBtn = document.getElementById('search-trigger-btn');
    const pills = document.querySelectorAll('.filter-pill');
    const container = document.getElementById('destinations-grid-container');
    const counterBox = document.getElementById('search-status-counter');
    const counterNum = document.getElementById('counter-number');
    const noResultsEl = document.getElementById('no-search-results-msg');

    let fullIndex = null;
    const initialHtml = container ? container.innerHTML : '';
    let activeFilter = 'all';
    let searchQuery = '';
    let currentRenderCount = 24;
    let currentFilteredList = [];
    let debounceTimer = null;

    // Detect current locale accurately from URL pathname
    function detectLocale() {
      const parts = window.location.pathname.split('/').filter(Boolean);
      if (parts.length > 0) {
        const first = parts[0];
        const supported = ['vi', 'de', 'fr', 'es', 'it', 'ja', 'ko', 'zh-tw', 'zh-cn', 'pt', 'ru'];
        if (supported.includes(first)) return first;
      }
      return 'en';
    }

    const currentLocale = detectLocale();
    const isVietnamese = currentLocale === 'vi';
    const fallbackImage = '/media/GYG_FR_PARIS_seine-river-gourmet-dinner-cruise-bateaux-parisiens_sunset-eiffel-landscape-4k.jpg';

    // Lazy load 1000 search index with caching
    async function loadIndex() {
      if (fullIndex) return fullIndex;
      try {
        const res = await fetch('/data/destinations_search_index.json');
        if (res.ok) {
          fullIndex = await res.json();
          return fullIndex;
        }
      } catch (e) {
        console.warn('Could not load 1000 search index:', e);
      }
      return null;
    }

    function createCardHtml(item, index = 0) {
      const gygUrl = window.getAttributedGygUrl ? window.getAttributedGygUrl(item.u) : item.u;
      const detailUrl = item.d 
        ? (currentLocale === 'en' ? `/experience/${item.k}/` : `/${currentLocale}/experience/${item.k}/`)
        : gygUrl;
      const ctaText = item.d ? (isVietnamese ? 'Xem Cẩm Nang' : 'Read Guide') : (isVietnamese ? 'Đặt Chỗ VIP' : 'Reserve VIP');
      
      let isSaved = false;
      try {
        const saved = JSON.parse(localStorage.getItem('t4u_bookmarks') || '[]');
        isSaved = saved.includes(item.k);
      } catch (e) {
        isSaved = false;
      }

      const isAboveFold = index < 12;
      const loadingAttr = isAboveFold ? 'loading="eager"' : 'loading="lazy"';
      const fetchPriorityAttr = index < 3 ? 'fetchpriority="high"' : '';

      return `
        <div
          class="destination-card-item group bg-[#0b1726] border border-slate-800 rounded-2xl overflow-hidden hover:border-[#c9a54e]/60 transition-all duration-300 hover:shadow-2xl hover:shadow-[#c9a54e]/10 flex flex-col h-full relative animate-fadeIn"
          data-title="${item.t}"
          data-location="${item.l}"
          data-region="${item.r}"
          data-category="${item.c}"
        >
          <div class="relative aspect-[16/10] overflow-hidden bg-slate-900">
            <img
              src="${item.m}"
              alt="${item.t}"
              ${loadingAttr}
              ${fetchPriorityAttr}
              onerror="this.onerror=null;this.src='${fallbackImage}'"
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500 ease-out"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-[#0b1726] via-transparent to-black/40"></div>
            
            <div class="absolute top-3.5 left-3.5 flex items-center space-x-2">
              <span class="bg-[#07111e]/90 backdrop-blur-md text-[#c9a54e] text-[11px] font-bold uppercase tracking-wider px-3 py-1 rounded-full border border-[#c9a54e]/30 shadow-md">
                ${item.l}
              </span>
            </div>

            <div class="absolute top-3.5 right-3.5 flex items-center space-x-1.5 z-10">
              <button
                type="button"
                class="bookmark-btn w-8 h-8 rounded-full bg-[#07111e]/90 backdrop-blur-md border border-slate-700 ${isSaved ? 'text-rose-500 border-rose-500/60' : 'text-slate-300'} hover:text-rose-500 hover:border-red-500/50 flex items-center justify-center transition-all shadow-md focus:outline-none"
                data-slug="${item.k}"
                title="Bookmark this experience"
                aria-label="Bookmark"
              >
                <svg class="heart-icon w-4 h-4" fill="${isSaved ? 'currentColor' : 'none'}" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                </svg>
              </button>

              <button
                type="button"
                class="card-share-btn w-8 h-8 rounded-full bg-[#07111e]/90 backdrop-blur-md border border-slate-700 text-slate-300 hover:text-[#c9a54e] hover:border-[#c9a54e]/50 flex items-center justify-center transition-all shadow-md focus:outline-none"
                data-url="${detailUrl}"
                data-title="${item.t}"
                title="Share"
                aria-label="Share"
              >
                <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"></path>
                </svg>
              </button>
            </div>

            <div class="absolute bottom-3 left-3.5">
              <span class="text-[10px] text-slate-300 block uppercase tracking-wider font-medium">${isVietnamese ? 'Khởi điểm' : 'Starting from'}</span>
              <span class="text-base font-bold text-white font-serif">${item.g}</span>
            </div>

            <div class="absolute bottom-3 right-3.5 flex items-center space-x-2">
              <span class="bg-[#07111e]/90 backdrop-blur-md text-amber-400 text-xs font-semibold px-2.5 py-1 rounded-full border border-slate-700 shadow-md flex items-center space-x-1">
                <span>★</span>
                <span>${item.s}</span>
              </span>
            </div>
          </div>

          <div class="p-5 flex flex-col flex-grow justify-between">
            <div>
              <span class="text-[10px] text-[#c9a54e] font-bold uppercase tracking-wider block mb-1">
                ${item.c}
              </span>
              <h3 class="font-serif text-base sm:text-lg font-bold text-white group-hover:text-[#c9a54e] transition-colors leading-snug mb-2 line-clamp-2">
                <a href="${detailUrl}" ${item.d ? '' : 'target="_blank" rel="nofollow sponsored noopener"'}>
                  ${item.t}
                </a>
              </h3>
            </div>

            <div class="pt-4 border-t border-slate-800/80 flex items-center justify-between gap-3">
              ${item.d ? `
                <a href="${detailUrl}" class="text-xs font-bold text-slate-300 hover:text-white flex items-center space-x-1 transition-colors">
                  <span>${ctaText}</span>
                  <span>→</span>
                </a>
              ` : `
                <span class="text-[11px] text-slate-400 font-medium">GetYourGuide Verified</span>
              `}

              <a
                href="${item.u}"
                target="_blank"
                rel="nofollow sponsored noopener"
                class="bg-[#ff7043] hover:bg-[#f4511e] text-white text-xs font-bold px-4 py-2 rounded-xl shadow-md shadow-[#ff7043]/20 hover:shadow-[#ff7043]/40 transition-all text-center flex items-center space-x-1"
              >
                <span>${isVietnamese ? 'Đặt Chỗ' : 'Reserve VIP'}</span>
                <span>→</span>
              </a>
            </div>
          </div>
        </div>
      `;
    }

    function renderCardsWithPagination() {
      if (!container) return;

      const toRender = currentFilteredList.slice(0, currentRenderCount);

      if (toRender.length === 0) {
        container.innerHTML = '';
        if (noResultsEl) noResultsEl.style.display = 'block';
        removeLoadMoreBtn();
      } else {
        if (noResultsEl) noResultsEl.style.display = 'none';
        container.innerHTML = toRender.map((item, idx) => createCardHtml(item, idx)).join('');
        
        // Render or update Load More Button
        if (currentFilteredList.length > currentRenderCount) {
          renderLoadMoreBtn(currentFilteredList.length);
        } else {
          removeLoadMoreBtn();
        }

        // Trigger immediate layout/scroll event so browsers calculate lazy images
        try {
          window.dispatchEvent(new Event('scroll'));
        } catch (e) {}
      }

      if (window.initBookmarks) window.initBookmarks();
    }

      if (window.initBookmarks) window.initBookmarks();
    }

    function renderLoadMoreBtn(total) {
      let loadMoreContainer = document.getElementById('load-more-container');
      if (!loadMoreContainer) {
        loadMoreContainer = document.createElement('div');
        loadMoreContainer.id = 'load-more-container';
        loadMoreContainer.className = 'col-span-full text-center py-10 flex flex-col items-center gap-2';
        container.parentNode.appendChild(loadMoreContainer);
      }

      const btnText = isVietnamese 
        ? `Khám Phá Thêm (${currentRenderCount}/${total}) ↓` 
        : `Load More Experiences (Showing ${currentRenderCount} of ${total}) ↓`;

      loadMoreContainer.innerHTML = `
        <button
          id="load-more-experiences-btn"
          type="button"
          class="bg-slate-900 hover:bg-[#0b1726] text-slate-200 hover:text-white border border-[#c9a54e]/50 hover:border-[#c9a54e] font-semibold text-xs sm:text-sm px-8 py-3.5 rounded-full shadow-lg transition-all duration-300 transform active:scale-95"
        >
          ${btnText}
        </button>
      `;

      const btn = document.getElementById('load-more-experiences-btn');
      if (btn) {
        btn.addEventListener('click', () => {
          currentRenderCount += 24;
          renderCardsWithPagination();
        });
      }
    }

    function removeLoadMoreBtn() {
      const el = document.getElementById('load-more-container');
      if (el) el.remove();
    }

    async function applyFilters() {
      // Toggle clear button
      if (clearBtn) {
        clearBtn.style.display = searchQuery ? 'flex' : 'none';
      }

      // If no query and filter is 'all', restore initial 10 curated cards
      if (!searchQuery && activeFilter === 'all') {
        if (container) container.innerHTML = initialHtml;
        if (noResultsEl) noResultsEl.style.display = 'none';
        if (counterBox) counterBox.classList.add('hidden');
        removeLoadMoreBtn();
        if (window.initBookmarks) window.initBookmarks();
        return;
      }

      const index = await loadIndex();
      if (!index) return;

      currentFilteredList = index.filter(item => {
        const text = (item.t + ' ' + item.l + ' ' + item.c).toLowerCase();
        const matchesSearch = !searchQuery || text.includes(searchQuery);

        let matchesFilter = true;
        if (activeFilter === 'europe') {
          matchesFilter = item.r.toLowerCase().includes('europe') || item.l.toLowerCase().includes('italy') || item.l.toLowerCase().includes('france') || item.l.toLowerCase().includes('swiss');
        } else if (activeFilter === 'asia') {
          matchesFilter = item.r.toLowerCase().includes('asia') || item.l.toLowerCase().includes('japan') || item.l.toLowerCase().includes('bali') || item.l.toLowerCase().includes('thailand');
        } else if (activeFilter === 'islands') {
          matchesFilter = item.r.toLowerCase().includes('islands') || item.l.toLowerCase().includes('maldives');
        } else if (activeFilter === 'safari') {
          matchesFilter = item.r.toLowerCase().includes('safari') || item.l.toLowerCase().includes('serengeti') || item.l.toLowerCase().includes('africa');
        } else if (activeFilter === 'me') {
          matchesFilter = item.r.toLowerCase().includes('middle east') || item.l.toLowerCase().includes('dubai');
        } else if (activeFilter === 'wellness') {
          matchesFilter = item.c.toLowerCase().includes('wellness') || item.c.toLowerCase().includes('spa');
        } else if (activeFilter === 'hotels') {
          matchesFilter = item.c.toLowerCase().includes('hotel') || item.c.toLowerCase().includes('sanctuaries');
        }

        return matchesSearch && matchesFilter;
      });

      currentRenderCount = 24; // Reset to first batch
      if (counterBox && counterNum) {
        counterNum.textContent = currentFilteredList.length;
        counterBox.classList.remove('hidden');
      }

      renderCardsWithPagination();
    }

    // Debounced input handler (150ms for ultra-smooth typing on mobile)
    if (searchInput) {
      searchInput.addEventListener('focus', loadIndex);
      searchInput.addEventListener('input', (e) => {
        clearTimeout(debounceTimer);
        debounceTimer = setTimeout(() => {
          searchQuery = e.target.value.toLowerCase().trim();
          applyFilters();
        }, 120);
      });
    }

    if (clearBtn) {
      clearBtn.addEventListener('click', () => {
        if (searchInput) searchInput.value = '';
        searchQuery = '';
        applyFilters();
        if (searchInput) searchInput.focus();
      });
    }

    pills.forEach(pill => {
      pill.addEventListener('mouseenter', loadIndex);
      pill.addEventListener('click', () => {
        pills.forEach(p => {
          p.classList.remove('active', 'bg-[#c9a54e]/20', 'border-[#c9a54e]', 'text-[#c9a54e]');
          p.classList.add('bg-slate-900/80', 'border-slate-700', 'text-slate-300');
        });
        pill.classList.remove('bg-slate-900/80', 'border-slate-700', 'text-slate-300');
        pill.classList.add('active', 'bg-[#c9a54e]/20', 'border-[#c9a54e]', 'text-[#c9a54e]');
        
        activeFilter = pill.getAttribute('data-filter') || 'all';
        applyFilters();
      });
    });

    if (searchBtn) {
      searchBtn.addEventListener('click', () => {
        const target = document.getElementById('experiences');
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', setup1000SearchAndFilters);
  } else {
    setup1000SearchAndFilters();
  }
</script>
